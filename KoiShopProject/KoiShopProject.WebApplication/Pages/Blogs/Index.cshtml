@page
@model KoiShopProject.WebApplication.Pages.Blogs.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Blogpost[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Blogpost[0].Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Blogpost[0].AuthorId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Blogpost[0].PostDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Blogpost[0].Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Blogpost) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuthorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Password)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BlogId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BlogId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BlogId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
